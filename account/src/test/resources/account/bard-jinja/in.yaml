{ # Generated by Bard #}
---
# This operation creates 10 accounts.
  { % for i in range(10, 20) % }
createAccount: {
  sender: gw1,
  target: vault,
  sendingTime:
    2023-01-20T10:00: { { i } },
  name: "account_{{ i }}",
  account: { { i } },
  currency: EUR,
  balance: 100
}
  { % endfor % }
...
---
# This operation creates 10 transfers between the accounts.
  { % for i in range(10, 20) % }
transfer: {
  sender: { { i } },
  target: { { i + 1 } },
  sendingTime:
    2023-01-20T10:00: + { { i } },
  currency: EUR,
  amount: 10,
  reference: "transfer_{{ i }}"
}
  { % endfor % }
...
